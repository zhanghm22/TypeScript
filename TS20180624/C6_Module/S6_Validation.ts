//----------------------Default exports

/*
  1. Each module can optionally export a default export. 
  2. Default exports are marked with the keyword default.
  3. There can only be one default export per module. 
  4. default exports are imported using a different import form.
*/

export default 123;